function square(a) {
  return (a * a);
}

// "Fat Arrow" notation for function definition
// let altSquare = a => {return (a * a)}

// Function Chaining demo
function emailParts(email) {
  return email.toLowerCase().split("@");
}
